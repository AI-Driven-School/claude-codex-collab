# ハンズオンチュートリアル - TODOアプリを作る

このチュートリアルでは、3AI適材適所テンプレートを使って**TODOアプリ**を0から本番デプロイまで完成させます。

---

## 目次

1. [プロジェクト作成](#1-プロジェクト作成)
2. [Phase 1: 要件定義（Claude）](#phase-1-要件定義claude)
3. [Phase 2: 設計（Claude）](#phase-2-設計claude)
4. [Phase 3-4: 実装・テスト（Codex）](#phase-3-4-実装テストcodex)
5. [Phase 5: レビュー（Claude）](#phase-5-レビューclaude)
6. [Phase 6: デプロイ（Claude）](#phase-6-デプロイclaude)
7. [完成](#完成)

---

## 1. プロジェクト作成

```bash
# テンプレートをインストール
curl -fsSL https://raw.githubusercontent.com/AI-Driven-School/claude-codex-collab/main/install-fullstack.sh | bash -s -- todo-app

# ディレクトリに移動
cd todo-app

# Claude Code起動
claude
```

起動後、以下のメッセージが表示されます:

```
┌─────────────────────────────────────────────────────────────┐
│   3AI適材適所テンプレート v6.0                               │
│   Claude（設計）→ Codex（実装）→ Claude（レビュー）         │
└─────────────────────────────────────────────────────────────┘
```

---

## Phase 1: 要件定義（Claude）

### コマンド実行

```
> /project TODOアプリ
```

### Claudeの出力

```
[1/6] 要件定義を生成中...（Claude）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 docs/requirements/todo-app.md を生成しました
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 生成される要件定義書

```markdown
# 要件定義: TODOアプリ

## ユーザーストーリー

AS A 一般ユーザー
I WANT TO タスクを作成・管理したい
SO THAT 日々のタスクを効率的に整理できる

## 受入条件

### 基本機能
- [ ] タスクを新規作成できる
- [ ] タスク一覧を表示できる
- [ ] タスクを完了状態に変更できる
- [ ] タスクを削除できる
- [ ] タスクを編集できる

### UI/UX
- [ ] レスポンシブデザイン（モバイル対応）
- [ ] キーボードショートカット（Enter で追加）
- [ ] 完了タスクは取り消し線で表示

### データ
- [ ] タスクはブラウザリロード後も保持される
- [ ] 作成日時が記録される

## 非機能要件

- パフォーマンス: 初回ロード 2秒以内
- アクセシビリティ: WCAG 2.1 AA準拠

## 制約事項

- フレームワーク: Next.js 14 (App Router)
- スタイリング: Tailwind CSS
- 状態管理: React hooks
- データ永続化: localStorage（Phase 1）
```

### 承認

```
承認しますか？ [Y/n/reject 理由]
> Y

✓ 要件定義を承認しました
```

---

## Phase 2: 設計（Claude）

### Claudeの出力

```
[2/6] 設計を生成中...（Claude）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 docs/specs/todo-list.md を生成しました
📄 docs/api/todo.yaml を生成しました
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 生成される画面設計書

```markdown
# 画面設計: TODOリスト

## 概要

タスクの一覧表示・追加・編集・削除を行うメイン画面。

## コンポーネント一覧

| コンポーネント | 種類 | 説明 |
|--------------|------|------|
| Header | Layout | アプリタイトル |
| TaskInput | Form | 新規タスク入力フィールド |
| TaskList | List | タスク一覧コンテナ |
| TaskItem | ListItem | 個別タスク表示 |
| Footer | Layout | 統計情報表示 |

## 状態

| 状態名 | トリガー | 表示内容 |
|-------|---------|---------|
| 初期表示 | ページロード | 保存済みタスク一覧 |
| 空状態 | タスク0件 | 「タスクがありません」 |
| 追加中 | 入力→追加 | 楽観的UI更新 |
| 編集中 | 編集クリック | インライン編集モード |

## インタラクション

### キーボードショートカット

| キー | アクション |
|------|----------|
| Enter | タスク追加 |
| Escape | 編集キャンセル |

### アニメーション

- タスク追加: フェードイン + スライドダウン
- タスク完了: チェックマークアニメーション
- タスク削除: フェードアウト + スライドアップ
```

### 承認

```
承認しますか？ [Y/n/reject 理由]
> Y

✓ 設計を承認しました

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 設計フェーズ完了！
  実装フェーズに進みます（Codexに委譲）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Phase 3-4: 実装・テスト（Codex）

設計承認後、**Codexが自動実行**します:

```
[3/6] 実装中...（Codex - full-auto）★メイン
      → src/app/page.tsx
      → src/components/todo/TaskInput.tsx
      → src/components/todo/TaskList.tsx
      → src/components/todo/TaskItem.tsx
      → src/lib/hooks/useTodos.ts
      → src/types/todo.ts

[4/6] テスト生成中...（Codex）
      → tests/todo.spec.ts
```

### 生成されるコード例

#### src/components/todo/TaskItem.tsx

```tsx
'use client';

import { useState } from 'react';
import { Todo } from '@/types/todo';

interface TaskItemProps {
  todo: Todo;
  onToggle: (id: string) => void;
  onDelete: (id: string) => void;
  onUpdate: (id: string, title: string) => void;
}

export function TaskItem({ todo, onToggle, onDelete, onUpdate }: TaskItemProps) {
  const [isEditing, setIsEditing] = useState(false);
  const [editTitle, setEditTitle] = useState(todo.title);

  const handleSubmit = () => {
    if (editTitle.trim()) {
      onUpdate(todo.id, editTitle.trim());
      setIsEditing(false);
    }
  };

  return (
    <div className={`
      group flex items-center gap-3 p-4 bg-white rounded-lg border
      transition-all duration-200
      ${todo.completed ? 'opacity-60' : 'hover:shadow-md'}
    `}>
      {/* チェックボックス */}
      <button
        onClick={() => onToggle(todo.id)}
        className={`
          w-6 h-6 rounded-full border-2 flex items-center justify-center
          ${todo.completed
            ? 'bg-green-500 border-green-500 text-white'
            : 'border-gray-300 hover:border-green-500'
          }
        `}
      >
        {todo.completed && (
          <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
          </svg>
        )}
      </button>

      {/* タイトル */}
      {isEditing ? (
        <input
          type="text"
          value={editTitle}
          onChange={(e) => setEditTitle(e.target.value)}
          onBlur={handleSubmit}
          onKeyDown={(e) => e.key === 'Enter' && handleSubmit()}
          className="flex-1 px-2 py-1 border rounded"
          autoFocus
        />
      ) : (
        <span className={`flex-1 ${todo.completed ? 'line-through text-gray-400' : ''}`}>
          {todo.title}
        </span>
      )}

      {/* 日付 */}
      <span className="text-sm text-gray-400">
        {new Date(todo.createdAt).toLocaleDateString('ja-JP')}
      </span>

      {/* アクション */}
      <div className="opacity-0 group-hover:opacity-100 flex gap-2">
        <button onClick={() => setIsEditing(true)} className="text-gray-400 hover:text-blue-500">
          編集
        </button>
        <button onClick={() => onDelete(todo.id)} className="text-gray-400 hover:text-red-500">
          削除
        </button>
      </div>
    </div>
  );
}
```

---

## Phase 5: レビュー（Claude）

```
[5/6] レビュー中...（Claude）
      → docs/reviews/todo-app.md
```

### 生成されるレビュー結果

```markdown
# コードレビュー: TODOアプリ

## サマリー

- ✅ 受入条件: 10/10 クリア
- ✅ テストカバレッジ: 85%
- ⚠️ 軽微な改善提案: 2件

## 改善提案

### 1. アクセシビリティ

削除ボタンに `aria-label` を追加することを推奨。

### 2. パフォーマンス

TaskItem のメモ化を推奨。

## テスト結果

✓ タスクを追加できる
✓ タスクを完了状態に変更できる
✓ タスクを削除できる
✓ タスクを編集できる
✓ 空のタスクは追加できない
✓ リロード後もタスクが保持される

Tests: 6 passed
```

---

## Phase 6: デプロイ（Claude）

```
[6/6] デプロイ準備完了

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

レビュー結果:
  ✅ 受入条件クリア
  ✅ テストパス
  ⚠️ 軽微な改善提案あり（デプロイに影響なし）

本番にデプロイしますか？ [Y/n]
> Y

デプロイ中...
  → Vercelにデプロイ

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ デプロイ完了！

  URL: https://todo-app-abc123.vercel.app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 完成

### 成果物一覧

```
todo-app/
├── docs/
│   ├── requirements/
│   │   └── todo-app.md          # 要件定義書（Claude）
│   ├── specs/
│   │   └── todo-list.md         # 画面設計書（Claude）
│   ├── api/
│   │   └── todo.yaml            # API設計（Claude）
│   └── reviews/
│       └── todo-app.md          # レビュー記録（Claude）
├── src/
│   ├── app/
│   │   └── page.tsx             # 実装（Codex）
│   ├── components/
│   │   └── todo/                # 実装（Codex）
│   └── types/
│       └── todo.ts              # 実装（Codex）
└── tests/
    └── todo.spec.ts             # テスト（Codex）
```

### AI役割分担

| フェーズ | AI | 生成物 |
|---------|-----|-------|
| 要件定義 | Claude | docs/requirements/ |
| 設計 | Claude | docs/specs/, docs/api/ |
| 実装 | **Codex** | src/ |
| テスト | **Codex** | tests/ |
| レビュー | Claude | docs/reviews/ |
| デプロイ | Claude | URL |

### 確認コマンド

```
> /status

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
プロジェクト: TODOアプリ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[1/6] 要件定義    ✅ 完了（Claude）
[2/6] 設計        ✅ 完了（Claude）
[3/6] 実装        ✅ 完了（Codex）★
[4/6] テスト      ✅ 完了（Codex）
[5/6] レビュー    ✅ 完了（Claude）
[6/6] デプロイ    ✅ 完了 → https://todo-app-abc123.vercel.app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 次のステップ

### 機能追加

```
> /project タスクのカテゴリ機能
```

### 大規模解析（Gemini）

```
> /analyze src/
```

### 技術リサーチ（Gemini）

```
> /research "タスク管理アプリのベストプラクティス"
```

---

## Tips

### 承認を却下して再生成

```
承認しますか？ [Y/n/reject 理由]
> reject もっとシンプルな設計にしてほしい

再生成中...（Claude）
```

### フェーズをスキップ

```
承認しますか？ [Y/n/reject 理由]
> skip

⚠️ 設計をスキップしました
```

### 進行中プロジェクトを再開

```
> /project --resume

進行中のプロジェクト:
  1. TODOアプリ (Phase 3: 実装中)

どれを再開しますか？
> 1
```
